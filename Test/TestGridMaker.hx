package;


class TestGridMaker extends haxe.unit.TestCase {
	
	public function testBasic() {
		var t = new GridMaker();
		assertEquals("A", "A");

		// check to see that the right grid was made
		// row * cols should equal the size of the array of items
		
		// check the genGrid worked and cleared the items

	}

	public function new() {
		super();
	}

}